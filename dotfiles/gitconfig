[user]
  email = deivid.rodriguez@gmail.com
  name = David Rodríguez

[alias]
  co = checkout
  ci = commit
  st = status
  br = branch
  ls = log --pretty=format:'%C(green)%h %C(yellow)[%ar]%Cred%d %Creset%s %Cblue[%cn]'
  lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit
  type = cat-file -t
  dump = cat-file -p

[core]
  editor = nvim
  autocrlf = input
  quotepath = off

[content]
  editor = nvim -w

[push]
  default = simple

[hub]
  protocol = ssh

[fetch]
  prune = true
